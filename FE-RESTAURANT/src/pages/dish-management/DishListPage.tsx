import React from 'react';
import DishListPage from '../../pages/DishListPage';

// Tạm thời dùng lại DishListPage cũ để đảm bảo hoạt động ổn định
const DishListPageWrapper: React.FC = () => {
  return <DishListPage />;
};

export default DishListPageWrapper;
